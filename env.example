# Optional: bake API URL at build time (overrides /app-config.json if set).
# For Azure Static Web Apps, you can leave this unset and use public/app-config.json + CI instead.
# VITE_API_URL=https://your-app.azurewebsites.net
