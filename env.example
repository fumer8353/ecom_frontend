# Public API base URL (no trailing slash). Required for production builds (e.g. Azure App Service URL).
# Vite inlines this at build time — set the same variable in your CI / Azure Static Web Apps build settings.
VITE_API_URL=https://your-api-app.azurewebsites.net
